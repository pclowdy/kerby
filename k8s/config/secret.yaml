apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  # NOTE: In a real production system, NEVER commit these raw values to git.
  # Use SOPS (Mozilla), SealedSecrets (Bitnami), or External Secrets Operator (AWS/GCP/Vault).
  DB_USER: "admin"
  DB_PASSWORD: "supersecretpassword"
  JWT_SECRET: "my-256-bit-secret"
